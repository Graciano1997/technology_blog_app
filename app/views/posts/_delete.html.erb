<%
=begin%>
 <section class="deleteSection">
  <%= form_with url: "/users/#{session[:user]['id']}/posts/#{post_item.id}/likes" do |form| %>
    <%= form.hidden_field :post_item_id, value: post_item.id %>
    <%= button_tag type: 'submit', class:'likeButton' do %>
      <%= image_tag "trash", alt: 'Delete', class: 'like-icon' %>
    <% end %>
  <% end %>
</section> 
<%
=end%>

<section class="deleteSection">
<%= form_with(url: "/delete/post/#{post_item.id}", method: :delete) do |form| %>
  <%= form.hidden_field :post_item_id, value: post_item.id %>
  <%= button_tag type: 'submit', class:'likeButton' do %>
    <%= image_tag "trash", alt: 'Delete', class: 'like-icon' %>
  <% end %>
<% end %>
</section>

